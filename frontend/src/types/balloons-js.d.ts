// Type declarations for packages without TypeScript support

declare module "balloons-js" {
  /**
   * Launches a full-screen balloon animation.
   * Creates a canvas overlay that automatically removes itself after the animation.
   */
  export function balloons(): void;
}
